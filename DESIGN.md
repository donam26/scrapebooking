---
name: ScrapeBooking
description: Thương hiệu tím than cho dịch vụ đếm phòng còn và giá đối thủ trên Booking.com, trải từ trang giới thiệu vào dashboard
colors:
  night-ink: "#16123a"
  night-raised: "#211a52"
  scan-purple: "#673de6"
  scan-purple-deep: "#5025d1"
  lavender-glow: "#8c85ff"
  lavender-wash: "#ebe4ff"
  lavender-tint: "#f4f0ff"
  lilac: "#cfc6ff"
  hatch-stripe: "#d6cbff"
  sold-out-plum: "#2f1c6a"
  exact-amber: "#ffcd35"
  exact-amber-mid: "#ffe28a"
  exact-amber-light: "#fff3cc"
  tick-green: "#00b090"
  tick-green-deep: "#008a70"
  tick-green-text: "#007a63"
  tick-green-soft: "#ddf5ef"
  mist: "#f4f5ff"
  paper: "#ffffff"
  paper-tint: "#f8f8fd"
  sunken: "#eeeef9"
  ink: "#1d1e20"
  body-text: "#36344d"
  muted: "#5e6072"
  faint: "#8a89a0"
  hairline: "#e3e1f0"
  hairline-strong: "#cfcce4"
  chart-grid: "#ece9f7"
  on-dark-soft: "#d5d1ef"
  on-dark-muted: "#a7a2d2"
  danger: "#c2334f"
  danger-deep: "#9b1d38"
  danger-soft: "#fdecef"
  warning-deep: "#7a5200"
  warning-soft: "#fff4d1"
  series-1: "#2a78d6"
  series-2: "#e0662a"
  series-3: "#9c3d8f"
  series-4: "#1596b8"
  series-5: "#a8601c"
  series-6: "#ef7fae"
  series-7: "#4a5cc4"
  series-8: "#c8352b"
typography:
  display:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, -apple-system, Segoe UI, sans-serif"
    fontSize: "clamp(40px, 4.4vw, 64px)"
    fontWeight: 600
    lineHeight: 1.12
    letterSpacing: "-0.02em"
  headline:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "clamp(30px, 3.2vw, 44px)"
    fontWeight: 700
    lineHeight: 1.18
    letterSpacing: "-0.02em"
  headline-cta:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "clamp(32px, 3.6vw, 48px)"
    fontWeight: 700
    lineHeight: 1.18
    letterSpacing: "-0.02em"
  title:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "clamp(20px, 1.8vw, 26px)"
    fontWeight: 700
    lineHeight: 1.3
    letterSpacing: "-0.01em"
  title-sm:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "18px"
    fontWeight: 700
    lineHeight: 1.3
  body-lead:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "18px"
    fontWeight: 400
    lineHeight: 1.6
  body:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "17px"
    fontWeight: 400
    lineHeight: 1.6
  body-sm:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "15px"
    fontWeight: 400
    lineHeight: 1.6
  note:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.6
  label:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "12px"
    fontWeight: 700
    letterSpacing: "0.05em"
  button:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "16px"
    fontWeight: 700
    lineHeight: 1.2
  nav:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "15.5px"
    fontWeight: 500
  stat:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "32px"
    fontWeight: 700
    lineHeight: 1.1
    letterSpacing: "-0.02em"
    fontFeature: "\"tnum\" 1"
  data-cell:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "12.5px"
    fontWeight: 700
    fontFeature: "\"tnum\" 1"
  wordmark:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "19px"
    fontWeight: 800
    lineHeight: 1
    letterSpacing: "0.08em"
  app-page-title:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, -apple-system, Segoe UI, sans-serif"
    fontSize: "24px"
    fontWeight: 700
    lineHeight: 1.33
    letterSpacing: "-0.015em"
  app-lead:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "17px"
    fontWeight: 400
    lineHeight: 1.7
  app-section-title:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "17px"
    fontWeight: 700
    lineHeight: 1.5
  app-card-title:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "15px"
    fontWeight: 700
    lineHeight: 1.55
  app-stat:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "20px"
    fontWeight: 700
    lineHeight: 1.4
    letterSpacing: "-0.01em"
    fontFeature: "\"tnum\" 1"
  app-body:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "14px"
    fontWeight: 400
    lineHeight: 1.6
  app-body-sm:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.54
  app-label:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "12px"
    fontWeight: 600
    lineHeight: 1.47
  app-micro:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "11px"
    fontWeight: 600
    lineHeight: 1.45
  app-nav:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "15px"
    fontWeight: 500
  app-button:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "14px"
    fontWeight: 600
    lineHeight: 1
  app-cell:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "13px"
    fontWeight: 700
    lineHeight: 1
    fontFeature: "\"tnum\" 1"
  app-wordmark:
    fontFamily: "Be Vietnam Pro, ui-sans-serif, system-ui, sans-serif"
    fontSize: "13.5px"
    fontWeight: 800
    lineHeight: 1
    letterSpacing: "0.08em"
rounded:
  swatch: "5px"
  cell: "6px"
  control: "8px"
  control-lg: "10px"
  tile: "12px"
  float: "14px"
  card: "16px"
  sheet: "20px"
  media: "24px"
  cta: "28px"
  pill: "999px"
spacing:
  gutter: "clamp(16px, 4vw, 48px)"
  section-y: "clamp(72px, 9vw, 120px)"
  container: "1240px"
  container-nav: "1320px"
  head-max: "780px"
  head-gap: "clamp(40px, 5vw, 56px)"
  card-pad: "28px"
  sheet-pad: "22px"
  gap-card: "24px"
  gap-list: "12px"
  app-sidebar: "248px"
  app-drawer: "280px"
  app-container: "1560px"
  app-gutter-sm: "16px"
  app-gutter-md: "24px"
  app-gutter-lg: "32px"
  app-card-pad: "20px"
  app-block-gap: "24px"
  app-stack: "12px"
  app-strip-gap: "3px"
  app-strip-col-min: "28px"
  app-control: "36px"
  app-control-sm: "32px"
  app-nav-item: "40px"
components:
  button-primary:
    backgroundColor: "{colors.scan-purple}"
    textColor: "{colors.paper}"
    typography: "{typography.button}"
    rounded: "{rounded.control}"
    padding: "14px 28px"
  button-primary-hover:
    backgroundColor: "{colors.scan-purple-deep}"
    textColor: "{colors.paper}"
  button-primary-lg:
    backgroundColor: "{colors.scan-purple}"
    textColor: "{colors.paper}"
    rounded: "{rounded.control}"
    padding: "18px 44px"
  button-outline-dark:
    backgroundColor: "transparent"
    textColor: "{colors.paper}"
    rounded: "{rounded.control-lg}"
    padding: "10px 18px"
  button-ghost-dark:
    backgroundColor: "transparent"
    textColor: "{colors.paper}"
    typography: "{typography.button}"
    padding: "16px 6px"
  nav-bar:
    backgroundColor: "{colors.night-ink}"
    textColor: "{colors.paper}"
    typography: "{typography.nav}"
    height: "76px"
  nav-menu-panel:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.float}"
    padding: "8px"
  card:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.body-text}"
    rounded: "{rounded.card}"
    padding: "{spacing.card-pad}"
  board-sheet:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.sheet}"
    padding: "22px 22px 18px"
  detail-panel:
    backgroundColor: "{colors.night-ink}"
    textColor: "{colors.on-dark-soft}"
    rounded: "{rounded.sheet}"
    padding: "26px 28px 24px"
  badge:
    backgroundColor: "{colors.lavender-wash}"
    textColor: "{colors.scan-purple-deep}"
    rounded: "{rounded.pill}"
    padding: "4px 12px"
  badge-exact:
    backgroundColor: "{colors.exact-amber}"
    textColor: "{colors.ink}"
    rounded: "{rounded.pill}"
    padding: "4px 12px"
  data-cell-exact:
    backgroundColor: "{colors.exact-amber}"
    textColor: "{colors.ink}"
    typography: "{typography.data-cell}"
    rounded: "{rounded.cell}"
    size: "32px"
  data-cell-capped:
    backgroundColor: "{colors.lavender-wash}"
    textColor: "{colors.ink}"
    typography: "{typography.data-cell}"
    rounded: "{rounded.cell}"
    size: "32px"
  data-cell-hidden:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.cell}"
    size: "32px"
  data-cell-sold-out:
    backgroundColor: "{colors.sold-out-plum}"
    textColor: "{colors.paper}"
    rounded: "{rounded.cell}"
    size: "32px"
  faq-item:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.tile}"
    padding: "20px 24px"
  cta-panel:
    backgroundColor: "{colors.scan-purple}"
    textColor: "{colors.paper}"
    rounded: "{rounded.cta}"
    padding: "clamp(36px, 5vw, 64px)"
  channel-card:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.float}"
    padding: "16px 20px"
  feature-icon:
    backgroundColor: "{colors.lavender-wash}"
    textColor: "{colors.scan-purple}"
    rounded: "{rounded.tile}"
    size: "40px"
  photo-frame:
    backgroundColor: "{colors.night-raised}"
    rounded: "{rounded.card}"
  app-sidebar:
    backgroundColor: "{colors.night-ink}"
    textColor: "{colors.on-dark-soft}"
    width: "{spacing.app-sidebar}"
  app-nav-item:
    backgroundColor: "transparent"
    textColor: "{colors.on-dark-soft}"
    typography: "{typography.app-nav}"
    rounded: "{rounded.control}"
    height: "{spacing.app-nav-item}"
    padding: "0 12px"
  app-nav-item-active:
    backgroundColor: "rgba(255, 255, 255, 0.09)"
    textColor: "{colors.paper}"
  app-button-primary:
    backgroundColor: "{colors.scan-purple}"
    textColor: "{colors.paper}"
    typography: "{typography.app-button}"
    rounded: "{rounded.control}"
    height: "{spacing.app-control}"
    padding: "0 14px"
  app-button-primary-hover:
    backgroundColor: "{colors.scan-purple-deep}"
    textColor: "{colors.paper}"
  app-button-secondary:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    typography: "{typography.app-button}"
    rounded: "{rounded.control}"
    height: "{spacing.app-control}"
    padding: "0 14px"
  app-button-secondary-hover:
    backgroundColor: "{colors.paper-tint}"
    textColor: "{colors.ink}"
  app-button-quiet:
    backgroundColor: "transparent"
    textColor: "{colors.scan-purple}"
    typography: "{typography.app-button}"
    rounded: "{rounded.control}"
    height: "{spacing.app-control}"
  app-button-quiet-hover:
    backgroundColor: "{colors.lavender-tint}"
    textColor: "{colors.scan-purple-deep}"
  app-button-danger:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.danger}"
    typography: "{typography.app-button}"
    rounded: "{rounded.control}"
    height: "{spacing.app-control}"
  app-input:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    typography: "{typography.app-body}"
    rounded: "{rounded.control}"
    height: "{spacing.app-control}"
    padding: "0 12px"
  app-segmented:
    backgroundColor: "{colors.sunken}"
    rounded: "{rounded.control}"
    padding: "2px"
  app-segmented-on:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    typography: "{typography.app-body-sm}"
    rounded: "{rounded.cell}"
    height: "32px"
    padding: "0 12px"
  app-card:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.body-text}"
    rounded: "{rounded.tile}"
    padding: "{spacing.app-card-pad}"
  app-stat-cell:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    typography: "{typography.app-stat}"
    padding: "16px 20px"
  app-badge:
    backgroundColor: "{colors.sunken}"
    textColor: "{colors.muted}"
    typography: "{typography.app-label}"
    rounded: "{rounded.pill}"
    height: "22px"
    padding: "0 8px"
  app-badge-yours:
    backgroundColor: "{colors.tick-green-soft}"
    textColor: "{colors.tick-green-text}"
    rounded: "{rounded.pill}"
  app-badge-sold-out:
    backgroundColor: "{colors.sold-out-plum}"
    textColor: "{colors.paper}"
    rounded: "{rounded.pill}"
  app-table-head:
    backgroundColor: "{colors.paper-tint}"
    textColor: "{colors.muted}"
    typography: "{typography.app-label}"
    padding: "10px 16px"
  app-readout:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.tile}"
    padding: "14px"
    width: "300px"
  axis-weekend:
    backgroundColor: "{colors.sunken}"
    textColor: "{colors.ink}"
    rounded: "{rounded.cell}"
  axis-active:
    backgroundColor: "{colors.scan-purple}"
    textColor: "{colors.paper}"
    rounded: "{rounded.cell}"
  strip-cell-exact-scarce:
    backgroundColor: "{colors.exact-amber}"
    textColor: "{colors.ink}"
    typography: "{typography.app-cell}"
    rounded: "{rounded.cell}"
    height: "32px"
  strip-cell-exact-mid:
    backgroundColor: "{colors.exact-amber-mid}"
    textColor: "{colors.ink}"
    typography: "{typography.app-cell}"
    rounded: "{rounded.cell}"
    height: "32px"
  strip-cell-exact-plenty:
    backgroundColor: "{colors.exact-amber-light}"
    textColor: "{colors.ink}"
    typography: "{typography.app-cell}"
    rounded: "{rounded.cell}"
    height: "32px"
  strip-cell-hidden:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.muted}"
    rounded: "{rounded.cell}"
    height: "32px"
  strip-cell-sold-out:
    backgroundColor: "{colors.sold-out-plum}"
    textColor: "{colors.paper}"
    rounded: "{rounded.cell}"
    height: "32px"
  strip-cell-unknown:
    backgroundColor: "{colors.sunken}"
    textColor: "{colors.muted}"
    typography: "{typography.app-cell}"
    rounded: "{rounded.cell}"
    height: "32px"
  strip-cell-nodata:
    backgroundColor: "transparent"
    textColor: "{colors.hairline-strong}"
    rounded: "{rounded.cell}"
    height: "32px"
---

# Design System: ScrapeBooking

**Phạm vi.** Tài liệu này điều chỉnh cả hai bề mặt của ScrapeBooking. Ngày 2026-09-25 chủ sản phẩm chọn nối thương hiệu của trang giới thiệu vào dashboard, nên hai bề mặt là một hệ thống với hai giọng:

- **Trang giới thiệu (giọng Brand):** route `/`, nhóm route `dashboard/src/app/(marketing)/`, mọi lớp mang tiền tố `lp-` trong `landing.css`.
- **Ứng dụng (giọng Operate):** nhóm route `dashboard/src/app/(app)/` (màn hình tenant và operator) cùng `/login`. Token là các biến `--sb-*` trong `dashboard/src/app/globals.css`, ánh xạ sang tiện ích Tailwind qua `@theme` (`bg-night`, `bg-canvas`, `text-muted`, `bg-exact`…), cộng các lớp `sb-mark-*`, `sb-cell`, `sb-select`, `sb-skeleton`, `sb-scroll`.

Hai bề mặt dùng chung bảng màu, bốn dấu mức tin cậy, một họ chữ Be Vietnam Pro, số thẳng cột, tím cho hành động và xanh cho khách sạn của bạn. Chúng khác nhau ở mật độ và vật liệu: trang giới thiệu bo 16–28px, có quầng tím trên nền tối, chữ thân 17px; app bo tối đa 12px, không quầng, chữ thân 14px, khung là thanh bên tím than. Mục nào chỉ áp cho một bề mặt thì ghi rõ "(trang giới thiệu)" hoặc "(app)". Trong frontmatter, typography, spacing và components mang tiền tố `app-`, `axis-`, `strip-` chỉ dùng trong app; phần còn lại là của trang giới thiệu hoặc dùng chung.

## Overview

**Creative North Star: "Đếm ô cửa sáng"**

Logo là một mặt tiền 3×2 ô cửa, hai ô sáng màu vàng; dải ảnh giữa trang viết "Mỗi ô cửa sáng là một phòng chưa bán". Cả hệ thống kể đúng một hình ảnh đó: một thị trường khách sạn về đêm (nền tím than có quầng tím), và những ô sáng được đếm lại ba lần mỗi ngày. Khung trang giới thiệu là landing SaaS chuẩn ngành theo tham chiếu Hostinger, do người dùng chọn thay cho hướng "tranh in đá Đông Dương": đầu trang tối có ảnh ghép sản phẩm, các phần sáng xen kẽ với thẻ trắng bo tròn, khối lời mời tím, chân trang tối. Chuẩn ngành là lựa chọn có chủ ý: khách sạn vãng lai phải nhận ra ngay đây là một dịch vụ phần mềm nghiêm túc, rồi mới đọc tới phần dữ liệu.

Chỗ riêng của hệ thống nằm ở dữ liệu. Sản phẩm tự trình bày: các mảnh giao diện thật (bảng phòng mini, bản tin đang gõ, công tắc lượt quét) nổi trên ảnh bờ biển Mỹ Khê; bảng 30 đêm bấm được; bản tin mẫu có bằng chứng. Mọi con số phòng còn mang một trong bốn dấu mức tin cậy, và dấu đó giống nhau ở chú giải, bảng lớn, bảng mini, bảng chi tiết và mọi màn hình app. Dữ liệu minh hoạ luôn được ghi rõ là minh hoạ.

Mật độ hai tầng trên trang giới thiệu: khoảng thở rộng ở cấp phần (đệm dọc tới 120px, đầu phần căn giữa, rộng tối đa 780px), dày đặc có kiểm soát ở cấp dữ liệu (ô 32px, 30 cột, số thẳng cột). Chỉ một họ chữ, Be Vietnam Pro, dấu tiếng Việt chuẩn ở mọi cỡ.

Trong app, cùng thế giới đó chuyển sang giọng Operate: một bảng thị trường theo từng khách sạn. Khung là thanh bên tím than 248px mang logo mặt tiền; nền làm việc là sương tím; mỗi khách sạn là một thẻ trắng bo 12px chứa dải 30 đêm mang bốn dấu và một đường giá đặt trên cùng trục đêm. App từ chối giao diện admin Tailwind mặc định: không bảng số trơn, không nhãn kỹ thuật phơi ra cho chủ khách sạn. Luồng đọc: mở app → đọc đêm nay và 7 đêm tới trong một dải số đo → dò một đêm qua cả chồng → bấm ô vào chi tiết đêm → đọc bản tin AI với bằng chứng bấm được. Chữ ký là "dò một đêm": rê chuột hoặc Tab vào một cột đêm, cột đó sáng qua mọi thẻ và một bảng đọc liệt kê mọi khách sạn đêm đó.

**Key Characteristics:**
- Nhịp nền (trang giới thiệu): tím than → sương tím nhạt / trắng xen kẽ → khối tím → tím than.
- Tím `#673de6` là màu hành động duy nhất; vàng, tím đậm, gạch chéo, viền đứt là mã dữ liệu, không phải trang trí.
- Thẻ trắng bo 16–20px nổi nhẹ trên nền sáng; quầng tím chỉ sống trên nền tối và khối lời mời (trang giới thiệu).
- Ảnh chụp màu thật, bo góc, có ghi nguồn Unsplash ở chân trang.
- Giao diện sản phẩm là hình minh hoạ chính, không có hình vẽ trừu tượng.
- Chuyển động ít và có nghĩa: bản tin gõ chữ, lượt quét 06:00 → 14:00 → 22:00 chạy vòng; tắt hết khi người dùng yêu cầu giảm chuyển động.
- App: thanh bên tím than, nền sương, thẻ trắng bo 12px viền đường tóc, bóng thẻ dịu; không quầng, không kính.
- App: một trục đêm chung, dính trên cùng, cho cả chồng thẻ khách sạn; vàng đậm dần khi phòng càng ít.
- App: thông tin kỹ thuật (token, chi phí, mô hình AI, probe, chặn, lỗi, mã lượt quét) chỉ operator thấy.

## Colors

Bảng màu một điểm nhấn tím trên nền tím than và sương tím, cộng một bộ mã dữ liệu bốn dấu được giữ nghiêm ngặt; app thêm một thang trung tính dày hơn, hai màu trạng thái và một bảng chuỗi biểu đồ đã kiểm định.

### Primary
- **Tím lượt quét / Scan Purple** (`scan-purple`, #673de6): nút chính, vòng chọn ô trên bảng, lượt quét đang chạy trong công tắc, mũi tên và chevron, cột đỉnh trong biểu đồ PMS, chữ thứ bảy/chủ nhật ở đầu cột bảng (chỉ trang giới thiệu), nền khối lời mời, thanh dòng thời gian, số giờ quét. Cũng là màu bôi đen chữ (`::selection`) và vòng focus mặc định. Trong app: nút chính, liên kết "Chi tiết →" và "Đêm 26/09", gạch dưới tab đang chọn, cột đêm đang dò trên trục, viền ô đang dò, vạch chỉ trong biểu đồ.
- **Tím lượt quét đậm** (`scan-purple-deep`, #5025d1): trạng thái hover của nút chính; chữ trên huy hiệu nền tím nhạt; chữ nhãn "Đề xuất" trong bản tin app.
- **Tím quầng / Lavender Glow** (`lavender-glow`, #8c85ff): viền nút phụ trên nền tối, gạch chân hover của liên kết điều hướng, nhãn chữ hoa trong bảng chi tiết, viền hộp bản tin ở đầu trang, viền đứt của ô "Ẩn", điểm cuối gradient thanh dòng thời gian, vòng focus trên nền tối. Trong app: biểu tượng của mục điều hướng đang chọn trên thanh bên, viền focus trên thanh bên.
- **Tím rửa / Lavender Wash** (`lavender-wash`, #ebe4ff): nền huy hiệu, nền ô biểu tượng tính năng và biểu tượng kênh liên hệ, nền của ô "Ít nhất". Trong app: vòng tròn biểu tượng của trạng thái rỗng, nhãn tông tím.
- **Tím phớt / Lavender Tint** (`lavender-tint`, #f4f0ff, app): nền hover của nút lặng và liên kết tím, hộp "Đề xuất" trong bản tin, ghi chú loại thông tin, dòng sự kiện đang được trỏ tới, nền cột đêm đang dò trong thước đo hết phòng.

### Secondary
- **Vàng ô cửa sáng / Exact Amber** (`exact-amber`, #ffcd35): "số phòng biết chắc". Ô "Chính xác" trên mọi bảng, huy hiệu "Chỉ còn 2 phòng", ô sáng trên logo, chip sự kiện "Sắp hết phòng", chấm tín hiệu mức vừa, trạng thái phòng còn trong bảng chi tiết (chữ vàng trên nền vàng 16%). Nền tối và chữ mực luôn đi kèm; không bao giờ đặt chữ trắng trên vàng.
- **Vàng vừa** (`exact-amber-mid`, #ffe28a, app) và **Vàng nhạt** (`exact-amber-light`, #fff3cc, app): hai bậc nhạt hơn của cùng dấu "Chính xác" trong dải khách sạn, để độ khan hiếm đọc được bằng mắt (xem Quy tắc Vàng theo độ khan). Chữ luôn là mực.

### Tertiary
- **Xanh dấu tích / Tick Green** (`tick-green`, #00b090): "của bạn và đã xác nhận". Dấu tích trong danh sách lợi ích, chấm đầu dòng ở thanh thông số, chấm "Của bạn" trên bảng, chấm sống cạnh "Lượt quét gần nhất", đường công suất PMS của bạn. Trong app: chấm 10px có quầng trước tên khách sạn của bạn, viền thẻ khách sạn của bạn (35%), chấm "Quét lúc…" ở đầu trang và thanh bên, chấm nhóm "Khách sạn của bạn" trong cài đặt.
- **Xanh dấu tích đậm** (`tick-green-deep`, #008a70): chữ trên nền xanh nhạt (chip mức tin cậy cao, số dòng hợp lệ khi nhập PMS). Trong app: nét đường giá của khách sạn bạn trên mọi dải và biểu đồ thị trường.
- **Xanh chữ** (`tick-green-text`, #007a63, app) trên **Xanh nhạt** (`tick-green-soft`, #ddf5ef, app): nhãn "Của bạn", nhãn tông xanh (tin cậy cao), số đo tông "tốt", tên khách sạn của bạn trong bảng đọc, thông báo thêm thành công.

### Data encoding (mã mức tin cậy)
- **Tím đậm hết phòng / Sold-out Plum** (`sold-out-plum`, #2f1c6a): ô "Hết phòng", chữ "HẾT" trắng 9.5px đậm 800. Cũng là ô phòng đã bán trong thẻ so sánh. Trên bảng mini kính mờ ở đầu trang dùng biến thể tối hơn để còn nổi trên ảnh. Trong app: ô "HẾT" 9px, cột thước đo "đối thủ hết phòng mỗi đêm" trong thẻ thị trường, nhãn sự kiện "Hết phòng" (chấm vàng).
- **Sọc gạch / Hatch Stripe** (`hatch-stripe`, #d6cbff): sọc 2px nghiêng 45° xen với nền tím rửa 4px, tạo ô "Ít nhất".
- **Tím sữa / Lilac** (`lilac`, #cfc6ff): cột "đối thủ hết phòng" trong biểu đồ PMS và ô chú giải của nó, viền hover của câu hỏi FAQ, thanh cuộn của bảng (trang giới thiệu).

### Neutral
- **Tím than đêm / Night Ink** (`night-ink`, #16123a): nền đầu trang, thanh điều hướng, phần Bản tin, bảng chi tiết, chân trang, nền `body` khi ở trang giới thiệu. Trong app: thanh bên, thanh trên và ngăn kéo trên điện thoại, nửa thương hiệu của trang đăng nhập, lớp phủ 55% sau ngăn kéo.
- **Tím than nổi / Night Raised** (`night-raised`, #211a52): nền chờ của khung ảnh và thẻ ảnh đầu trang.
- **Sương tím / Mist** (`mist`, #f4f5ff): nền các phần sáng xen kẽ; nền mục nhận định trong bản tin; nền thước đo cột compset. Trong app: nền làm việc (`body`) của mọi màn hình, nền trục đêm dính.
- **Giấy / Paper** (`paper`, #ffffff): thẻ, bảng, bản tin, các phần trắng xen kẽ, chữ trên nền tối và tím. Trong app: mọi thẻ, ô nhập, nút phụ, nửa biểu mẫu của trang đăng nhập.
- **Giấy phớt / Paper Tint** (`paper-tint`, #f8f8fd, app): đầu bảng, hover hàng, trạng thái rỗng, ghi chú trung tính, tiêu đề nhóm lượt quét, ô nhập bị vô hiệu.
- **Lõm / Sunken** (`sunken`, #eeeef9, app): rãnh của nhóm chọn, ô cuối tuần trên trục đêm, ô "Không đọc được", nhãn xám, đếm số trên tab, nền skeleton.
- **Mực / Ink** (`ink`, #1d1e20): tiêu đề và số liệu trên nền sáng.
- **Chữ thân / Body Text** (`body-text`, #36344d): văn bản thường trên nền sáng. Trong app cũng là nét đường giá của khách sạn đối thủ và trung vị đối thủ.
- **Chữ phụ / Muted** (`muted`, #5e6072): đoạn dẫn đầu phần, ghi chú, chú giải, nhãn trục.
- **Chữ mờ / Faint** (`faint`, #8a89a0, app): placeholder, gợi ý thao tác, chữ bị vô hiệu, dấu chấm ngăn, nét "thấp nhất đối thủ".
- **Đường tóc / Hairline** (`hairline`, #e3e1f0): đường chia 1px trong thẻ, viền thẻ biểu đồ, viền FAQ, vạch ngăn hàng "Của bạn". Trong app: viền mọi thẻ, đường chia bảng, khe giữa các ô của dải số đo.
- **Đường tóc đậm** (`hairline-strong`, #cfcce4, app): viền ô nhập, vòng nút phụ, thanh cuộn, đường đáy biểu đồ, chữ gạch trong ô "Chưa quét".
- **Lưới biểu đồ** (`chart-grid`, #ece9f7): đường lưới ngang của mọi biểu đồ ở cả hai bề mặt, viền nhãn mức giá.
- **Chữ mờ trên nền tối / On-dark Soft** (`on-dark-soft`, #d5d1ef): văn bản thân trên nền tím than, thanh thông số, chú thích trong bảng chi tiết, mục điều hướng chưa chọn trên thanh bên.
- **Chữ nhạt trên nền tối** (`on-dark-muted`, #a7a2d2, app): biểu tượng điều hướng chưa chọn, nhãn "Khách sạn", "Vận hành", "Lượt tiếp theo…", vai trò người dùng.

### Trạng thái (app)
- **Đỏ lỗi** (`danger`, #c2334f) với `danger-deep` (#9b1d38) và `danger-soft` (#fdecef): hộp lỗi, nút nguy hiểm (xoá, thu hồi), viền ô nhập sai, số đo tông "xấu", nhãn tông đỏ. Không dùng cho dữ liệu phòng.
- **Nâu cảnh báo** (`warning-deep`, #7a5200) trên `warning-soft` (#fff4d1): ghi chú cảnh báo, chip biến động giảm trong dòng sự kiện, nhãn tin cậy vừa, số đo tông "chú ý" ("Thấp hơn 47%"). Đây là họ màu riêng, không phải vàng "Chính xác".

### Chuỗi biểu đồ (app)
Tám màu phân loại `series-1` → `series-8` (xanh dương, cam, mận, xanh ngọc, nâu đồng, hồng, chàm, đỏ gạch), đã qua kiểm tra mù màu, độ sáng và độ bão hoà, gán theo thứ tự cố định cho từng loại phòng trong biểu đồ lịch sử phòng còn và giá.

### Named Rules
**Quy tắc Bốn dấu.** Mọi con số phòng còn đều mang đúng một trong bốn dấu: vàng đặc = chính xác; sọc tím nhạt = ít nhất (số trong ô là mức sàn, hiển thị "≥N" ở bảng chi tiết); nền trắng viền đứt 1.5px tím quầng = ẩn (còn phòng, không lộ số, ô để trống); tím đậm có chữ "HẾT" = hết phòng. Không có dấu thứ năm, không hiện số mà không có dấu, và bảng nào dùng dấu thì có chú giải ngay bên dưới. Trong app, hai trạng thái chất lượng dữ liệu đứng ngoài bốn dấu và không bao giờ mang số phòng: ô lõm xám có "?" = không đọc được (bị chặn hoặc lỗi); ô trong suốt viền đường tóc = chưa quét. Chú giải mức khách sạn liệt kê cả hai; chú giải mức loại phòng liệt kê đúng bốn dấu.

**Quy tắc Tím là để bấm.** Tím lượt quét đánh dấu thứ người dùng có thể bấm hoặc đang được chọn. Hết phòng dùng tím đậm `#2f1c6a`, đủ xa để hai nghĩa không lẫn vào nhau.

**Quy tắc Xanh là của bạn.** Xanh dấu tích chỉ gắn với khách sạn của người xem và những điều đã xác nhận. Không dùng xanh để mã hoá tình trạng phòng của đối thủ.

**Quy tắc Vàng theo độ khan (app).** Trong dải khách sạn, ô "Chính xác" đậm dần khi phòng càng ít: ≤3 phòng vàng đặc `exact-amber`, 4–10 phòng `exact-amber-mid`, >10 phòng `exact-amber-light`. Cả ba vẫn là một dấu; số luôn in trong ô, chữ mực.

**Quy tắc Cuối tuần là mực (app).** Thứ bảy và chủ nhật trên trục đêm là chữ mực đậm 800 trên ô lõm, không bao giờ tím, để tím chỉ còn nghĩa "đang chọn".

**Quy tắc Chuỗi không mượn nghĩa (app).** Bảng chuỗi biểu đồ không có xanh lá (của bạn) và vàng (chính xác). Giá trị là mức sàn "≥N" vẽ bằng chấm rỗng, kèm chú giải "Chấm rỗng: ít nhất".

## Typography

**Display Font:** Be Vietnam Pro (dự phòng ui-sans-serif, system-ui, -apple-system, Segoe UI, sans-serif)
**Body Font:** Be Vietnam Pro
**Label/Mono Font:** không có; số liệu dùng Be Vietnam Pro với `tabular-nums`

**Character:** Sans hình học do nhà thiết kế Việt làm, nạp qua `next/font` với tập latin + vietnamese, trọng lượng 400–800 (trong app qua biến `--font-app` ở `layout.tsx`, ánh xạ vào `--font-sans`). Thứ bậc đến từ cỡ và độ đậm, không từ họ chữ thứ hai: tiêu đề chính 600 cho cảm giác mở, tiêu đề phần 700 chắc, 800 chỉ cho logo và vài con số điểm nhấn.

### Hierarchy
Trang giới thiệu:
- **Display** (600, clamp(40px, 4.4vw, 64px), 1.12, −0.02em, `text-wrap: balance`): chỉ H1 trắng ở đầu trang.
- **Headline** (700, clamp(30px, 3.2vw, 44px), 1.18, −0.02em, balance): H2 của mỗi phần. Khối lời mời dùng bản lớn hơn clamp(32px, 3.6vw, 48px). Dải ảnh cửa sổ dùng cùng kiểu ở clamp(26px, 3vw, 42px) cho câu trích.
- **Title** (700, clamp(20px, 1.8vw, 26px), 1.3, −0.01em): H3 dẫn khối mức tin cậy. Tên khách sạn trong bảng chi tiết 26px/700.
- **Title nhỏ** (700, 16.5–18.5px, 1.3–1.35): tiêu đề trong thẻ: tên thẻ so sánh, bước dòng thời gian, nguyên tắc, mục chú giải, nhận định bản tin, tiêu đề bảng và biểu đồ (17px).
- **Body lead** (400, 18px, 1.6, màu chữ phụ): đoạn dẫn dưới H2 và đoạn chính của bố cục hai cột, tối đa 56ch. Danh sách dấu tích ở đầu trang cũng 18px.
- **Body** (400, 17px, 1.6): cỡ gốc của trang; 16px dưới 720px. Câu trả lời FAQ 16px, tối đa 68ch.
- **Body nhỏ** (400, 14.5–15.5px): mô tả trong dòng thời gian, nguyên tắc (tối đa 56ch), chú giải, chân trang.
- **Note** (400, 13–13.5px, chữ phụ): ghi chú "minh hoạ", chú giải bảng, meta lượt quét.
- **Label** (700, 12px, 0.04–0.06em, CHỮ HOA): chỉ làm tiêu đề cột bên trong mảnh giao diện sản phẩm ("Sáu lượt quan sát gần nhất", "Sự kiện của đêm này", nhãn hộp "Bản tin 07:30", nhãn tín hiệu trong bản tin).
- **Số liệu**: ô bảng 12.5px/700; con số lớn 32px/700/−0.02em (giá thẻ so sánh), clamp(28px, 3vw, 40px) cho "0/4 đối thủ hết phòng"; giờ quét trên dòng thời gian 22px/800 màu tím.
- **Wordmark** (800, 19px, 0.08em, chữ hoa "SCRAPEBOOKING"): 15px/0.05em dưới 720px.

App (thang Tailwind được đặt lại trong `@theme`: 2xs 11 · xs 12 · sm 13 · base 14 · md 15 · lg 17 · xl 20 · 2xl 24px):
- **Page title** (700, 24px, 1.33, −0.015em, balance): H1 duy nhất của mỗi màn hình ("Tổng quan", "Đăng nhập").
- **Lead** (400, 17px, 1.7, mực, tối đa 70ch): đoạn tóm tắt của bản tin AI. Câu thương hiệu trên nửa tối của trang đăng nhập dùng 600, 17→20→28px.
- **Section title** (700, 17px): H2 của từng phần trong bản tin, kèm số đếm 14px/600 chữ phụ.
- **Card title** (700, 15px): tiêu đề thẻ, tên khách sạn trên dải, đầu bảng đọc, tiêu đề trạng thái rỗng.
- **Stat** (700, 20px, −0.01em, thẳng cột; 17px dưới 640px): giá trị trong dải số đo.
- **Body** (400, 14px, 1.6): cỡ gốc của app, ô bảng, nội dung thẻ, ô nhập, nút.
- **Body nhỏ** (400–600, 13px): phụ đề, đường dẫn, nhãn trường (600), chữ trong nhóm chọn, trạng thái trên đầu thẻ.
- **Label** (600, 12px, chữ thường): đầu bảng, nhãn dải số đo, chú giải, nhãn huy hiệu, tiêu đề nhóm lượt quét.
- **Micro** (600, 11px): thứ và tháng trên trục đêm, số trên thước đo hết phòng, công suất PMS dưới dải.
- **Nav** (500, 15px): mục thanh bên.
- **Ô dải** (700, 13px, thẳng cột; "HẾT" 9px, 0.02em): số trong ô 32px.
- **Wordmark app** (800, 13.5px, 0.08em, "SCRAPEBOOKING"): 13px trên thanh trên điện thoại, 14px trên trang đăng nhập.

### Named Rules
**Quy tắc Một họ chữ.** Chỉ Be Vietnam Pro. Không thêm serif, mono hay họ chữ hiển thị thứ hai; cần nhấn thì đổi độ đậm hoặc cỡ. Ngoại lệ duy nhất (app): mono hệ thống `ui-monospace` 12px, không nạp font, cho mã máy (trigger key, mã lượt quét) trên màn hình chỉ operator thấy.

**Quy tắc Số thẳng cột.** Mọi con số được so sánh hoặc thay đổi theo thời gian (ô bảng, giá, giờ quét, trục biểu đồ, số liệu thống kê, kênh liên hệ) dùng `font-variant-numeric: tabular-nums`.

**Quy tắc Nhãn viết thường (app).** Mọi nhãn trong app viết như câu: đầu bảng, nhãn số đo, nhóm "Vận hành" trên thanh bên, nhãn huy hiệu. Chữ hoa chỉ dành cho wordmark và mã quốc gia; không có kicker hay eyebrow.

## Layout

Trang giới thiệu: khung một cột căn giữa, rộng tối đa 1240px (thanh điều hướng 1320px), lề hai bên clamp(16px, 4vw, 48px). Mỗi phần có đệm dọc clamp(72px, 9vw, 120px) và nền riêng; thứ tự nền hiện tại: tối (đầu trang) → sương → trắng → sương → tối (Bản tin) → trắng → sương → trắng → sương (lời mời) → tối (chân trang). Hai phần sáng cạnh nhau luôn khác tông sương/trắng để ranh giới hiện ra mà không cần đường kẻ.

Đầu phần căn giữa, rộng tối đa 780px: H2, cách 16px, đoạn dẫn 18px màu chữ phụ, rồi khoảng clamp(40px, 5vw, 56px) tới nội dung. Các bố cục hai cột dùng tỷ lệ lệch: đầu trang 1fr : 1.08fr, chữ : hình 5fr : 7fr, ảnh : nguyên tắc 4fr : 8fr, lời mời 7fr : 5fr, khoảng cách clamp(32–40px, 5–6vw, 72–88px). Lưới thẻ dùng khe 24px; danh sách xếp chồng dùng khe 12px.

**Điểm ngắt (trang giới thiệu):**
- **≤1180px:** liên kết điều hướng ẩn, hiện nút menu gập; bảng chi tiết 3 cột → 2 cột (cột loại phòng chiếm hai hàng).
- **≤1000px:** mọi bố cục hai cột (đầu trang, chữ/hình, nguyên tắc, lời mời) xếp thành một cột; ảnh ghép đầu trang tối đa 680px, bản tin tối đa 640px; dải chú giải 4 → 2 cột; dòng thời gian ngang chuyển thành dọc với thanh bên trái; ảnh nguyên tắc 16:9.
- **≤720px:** chữ gốc 16px; thanh điều hướng cao 64px, nút phụ bỏ biểu tượng, "Đăng nhập" chỉ còn biểu tượng; nút chính lớn rộng 100%; thanh thông số xếp dọc; thẻ so sánh, chú giải, nguyên tắc một cột; ảnh ghép đầu trang đổi tỷ lệ 4:4.2 và đặt lại các mảnh nổi; ẩn ảnh cà phê cạnh bản tin; bảng chi tiết một cột.

Trang không bao giờ cuộn ngang (`overflow-x: clip` trên gốc). Bảng 30 đêm cuộn ngang trong khung của nó, cột tên khách sạn dính bên trái, khung cuộn nhận focus bằng bàn phím. Mỗi phần có `scroll-margin-top: 76px` để liên kết neo không bị thanh điều hướng dính che.

**App.** Khung hai vùng: thanh bên tím than cố định 248px bên trái, vùng làm việc rộng tối đa 1560px căn giữa trong phần còn lại, lề 16px → 24px (≥640px) → 32px (≥1024px), đệm dọc 24px → 32px. Mỗi màn hình mở bằng đầu trang (đường dẫn nếu có, H1, phụ đề, thao tác bên phải, cách nội dung 24px). Nội dung xếp dọc: khối cách nhau 24px, thẻ trong một chồng cách nhau 12px. Tổng quan theo thứ tự cố định: đầu trang (tiêu đề, "30 đêm, T6 25/09 – T7 24/10", chấm xanh "Quét lúc…", chọn ngày và 14/30/60 đêm) → dải số đo bốn ô → chú giải bốn dấu → trục đêm dính → thẻ Thị trường → thẻ khách sạn của bạn → thẻ đối thủ → chú giải đường giá.

Hình học dải: mọi dải trong chồng dùng cùng lưới `n` cột bằng nhau, khe 3px, đệm ngang 20px, nên một đêm nằm đúng một cột qua trục, thước đo, dải ô và đường giá. Khi khung hẹp hơn `n × 28px + (n − 1) × 3px + 42px`, cả chồng cuộn ngang như một khối trong khung `sb-scroll` (tràn sát mép trên điện thoại): trục thôi dính, mỗi thẻ tự in hàng số ngày (cuối tuần đậm mực), trạng thái chuyển xuống dưới tên, tên khách sạn dính bên trái.

**Điểm ngắt (app, thang Tailwind):**
- **<1024px:** thanh bên ẩn; thay bằng thanh trên tím than cao 56px (nút menu, logo) và ngăn kéo; trục đêm dính dưới thanh trên (top 56px). Dải số đo 2 cột, ô lẻ cuối trải đủ hàng.
- **≥640px:** lề 24px, ô số đo rộng đệm hơn và số 20px; tab hiện biểu tượng.
- **≥768px:** dòng sự kiện thành lưới 4 cột (136px nhãn · tên · 118px đêm · 228px thay đổi); bảng hiện đủ cột.
- **≥1024px:** thanh bên cố định; dải số đo một hàng.
- **≥1280px:** đầu thẻ khách sạn hiện thêm "Giá kỳ này …".

App cũng không bao giờ cuộn ngang ở cấp trang; bảng, tab và chồng dải cuộn trong khung riêng với thanh cuộn mảnh 8px màu đường tóc đậm.

**Quy tắc Một trục (app).** Mọi thứ đo theo đêm trên một màn hình dùng chung một lưới cột; không thẻ nào tự co giãn trục riêng. Khi không vừa, cả chồng cuộn cùng nhau, không cuộn từng thẻ.

## Elevation & Depth

Hệ thống lai: chiều sâu chính đến từ lớp tông (tím than → sương → thẻ trắng), bóng chỉ đỡ các vật thể nổi. Thẻ trên nền sáng dùng một bóng dịu hai lớp có ánh tím. Vật thể nổi trên nền tối hoặc trên ảnh dùng bóng sâu, rơi dài, màu gần đen tím. Không có bóng cứng lệch khối. Trên nền tối của trang giới thiệu, quầng sáng hướng tâm màu tím thay cho bóng: đầu trang có ba quầng (mạnh nhất ở góc dưới phải, 80% độ đậm), phần Bản tin có hai quầng ở hai góc chéo, khối lời mời có quầng tím quầng và tím đậm trên nền tím.

App phẳng hơn: nền sương, thẻ trắng có viền đường tóc 1px cộng một bóng thẻ dịu, vậy thôi. Bóng nổi chỉ dành cho thứ nằm trên nội dung (bảng đọc một đêm, chú thích biểu đồ, ngăn kéo). Nút không có quầng và không nhấc khi hover. Quầng hướng tâm duy nhất trong phạm vi app nằm trên nửa tím than của trang đăng nhập (tím 45% góc dưới phải, tím quầng 18% góc trên trái), đúng Quy tắc Quầng chỉ ở nền tối.

### Shadow Vocabulary
Trang giới thiệu:
- **Thẻ dịu** (`box-shadow: 0 1px 2px rgba(29, 30, 32, 0.06), 0 12px 32px -12px rgba(47, 28, 106, 0.18)`): thẻ trắng, bảng 30 đêm, bảng chi tiết, dải ảnh, ảnh nguyên tắc, câu hỏi FAQ đang mở.
- **Nổi sâu** (`box-shadow: 0 40px 80px -30px rgba(6, 2, 30, 0.8)`): thẻ ảnh đầu trang, thẻ bản tin trên nền tối.
- **Nổi vừa** (`box-shadow: 0 24px 50px -20px rgba(6, 2, 30, 0.7)`): hộp bản tin đang gõ; menu gập dùng `0 24px 48px -16px rgba(6, 2, 30, 0.6)`; công tắc lượt quét `0 16px 32px -14px rgba(6, 2, 30, 0.6)`.
- **Kênh liên hệ** (`box-shadow: 0 16px 30px -18px rgba(22, 18, 58, 0.7)`, hover `0 22px 36px -18px rgba(22, 18, 58, 0.8)`): thẻ trắng trên khối tím.
- **Quầng nút** (`box-shadow: 0 10px 24px -10px rgba(103, 61, 230, 0.8)`): chỉ khi hover nút chính.
- **Nhấc ô** (`box-shadow: 0 6px 12px -6px rgba(47, 28, 106, 0.55)`): khi hover ô dữ liệu (dùng chung với app qua lớp `sb-cell`).

App:
- **Thẻ app** (`--sb-shadow-card: 0 1px 2px rgba(29, 30, 32, 0.05), 0 10px 28px -18px rgba(47, 28, 106, 0.22)`): mọi thẻ, dải số đo, thẻ khách sạn, thẻ nhận định.
- **Nổi app** (`--sb-shadow-float: 0 2px 6px rgba(22, 18, 58, 0.08), 0 24px 48px -16px rgba(22, 18, 58, 0.35)`): bảng đọc một đêm, chú thích biểu đồ, ngăn kéo điện thoại.
- **Đế nút chính** (`box-shadow: 0 1px 2px rgba(47, 28, 106, 0.25)`): nút chính lúc nghỉ; mất khi vô hiệu.
- **Nút nổi trong nhóm chọn** (`box-shadow: 0 1px 2px rgba(22, 18, 58, 0.12), 0 0 0 1px rgba(22, 18, 58, 0.04)`): lựa chọn đang bật.
- **Lõm ô nhập** (`box-shadow: inset 0 1px 1px rgba(22, 18, 58, 0.04)`).
- **Quầng chấm của bạn** (`box-shadow: 0 0 0 3px rgba(0, 176, 144, 0.18)`): chấm xanh trước tên khách sạn của bạn và chấm "Quét lúc…".

### Named Rules
**Quy tắc Quầng chỉ ở nền tối.** Quầng tím hướng tâm chỉ xuất hiện trên mặt tím than và trong khối lời mời tím. Phần sáng luôn phẳng: sương hoặc trắng đặc.

**Quy tắc Kính chỉ trên ảnh.** Lớp kính mờ (`backdrop-filter: blur`) chỉ dùng cho mảnh giao diện nổi trên ảnh đầu trang (bảng phòng mini, công tắc lượt quét), nơi nó để ảnh còn thấy được. Không dùng kính cho thẻ trên nền phẳng. Trong app, làm mờ nền chỉ xuất hiện sau dải dính đục 95% (trục đêm, tiêu đề nhóm lượt quét) và lớp phủ ngăn kéo (2px), để nội dung cuộn bên dưới không lẫn vào chữ; không bao giờ là một thẻ kính.

**Quy tắc Không quầng trong app.** Màn hình sau đăng nhập không có quầng, gradient trang trí hay bóng nút phát sáng; chiều sâu chỉ là sương → thẻ trắng viền đường tóc → vật nổi.

## Shapes

Trang giới thiệu: góc bo tăng theo kích thước vật thể: ô dữ liệu 6px (ô mini 3px, ô phòng thẻ so sánh 5px) → nút và chip bằng chứng 8px → nút phụ và nút menu 10px → ô biểu tượng, FAQ, mục nhận định 12px → menu gập, bảng mini, thẻ kênh liên hệ 14px → thẻ, khung ảnh 16px → bảng 30 đêm, bảng chi tiết, bản tin 20px → thẻ ảnh đầu trang, dải ảnh, ảnh nguyên tắc 24px → khối lời mời 28px. Huy hiệu, nhãn trạng thái, chip sự kiện và công tắc lượt quét là viên thuốc (999px). Hình tròn cho điểm dừng dòng thời gian (44px, viền 3px tím), số bước trong lời mời (30px), chấm trạng thái (8–9px).

Viền mảnh: đường chia 1px màu đường tóc trong thẻ; 1.5px cho viền có nghĩa (nút phụ, thẻ so sánh "nóng", viền đứt ô "Ẩn", viền hộp bản tin, viền bảng kính). Ảnh luôn cắt trong khung bo góc, `object-fit: cover`.

App dùng cùng logic ở thang nhỏ hơn và dừng ở 12px: mẫu dấu trong chú giải và bảng đọc 5px (mẫu 12px trong dòng sự kiện 3px) → ô dải, nút trong nhóm chọn, chip biến động, nhãn trục cuối tuần 6px → nút, ô nhập, nhóm chọn, mục thanh bên, hộp ghi chú, trạng thái rỗng, chip bằng chứng 8px → thẻ, dải số đo, bảng đọc 12px. Viên thuốc cho huy hiệu, nhãn "Của bạn", đếm số trên tab; hình tròn cho chấm trạng thái (6–10px), avatar 32px, vòng biểu tượng trạng thái rỗng 40px. Mọi thẻ app có viền 1px đường tóc; thẻ khách sạn của bạn đổi viền sang xanh 35% cộng vòng xanh 10%. Viền đứt chỉ có hai nghĩa: dấu "Ẩn" và khung "Nhận định bị loại".

**Quy tắc Bo theo cỡ.** Vật càng lớn góc càng tròn; không bo một ô dữ liệu tròn như thẻ, không bo khối lời mời vuông như nút.

**Quy tắc Trần 12px (app).** Không vật nào trong app bo quá 12px (trừ viên thuốc và hình tròn); bo 16–28px là giọng của trang giới thiệu.

## Components

### Buttons
Tự tin, phẳng, có một cú nhấc nhẹ (trang giới thiệu).
- **Shape:** góc bo vừa (8px).
- **Primary:** nền tím lượt quét, chữ trắng 16px/700, đệm 14px 28px; bản lớn 18px 44px, chữ 18px, rộng 100% dưới 720px. Mỗi khối chỉ một nút chính ("Liên hệ tư vấn").
- **Hover / Focus:** nền tím đậm, nhấc 1px, quầng nút; nhấn trở về 0. Focus: vòng 2px tím, lệch 3px, bo 4px; trên nền tối vòng đổi sang tím quầng, trên khối lời mời đổi sang trắng.
- **Outline trên nền tối:** viền 1.5px tím quầng, bo 10px, đệm 10px 18px, chữ trắng 15px/600 kèm biểu tượng chat; hover nền tím quầng 14% và viền sáng hơn.
- **Ghost trên nền tối:** chữ trắng 16px/700 kèm mũi tên, mũi tên trượt 4px khi hover ("Xem bảng mẫu").

### Chips / Badges
- **Huy hiệu:** viên thuốc, nền tím rửa, chữ tím đậm 13px/700, đệm 4px 12px. Biến thể "chính xác" nền vàng chữ mực.
- **Chip sự kiện (trên nền tối):** viên thuốc 12px/700, nền tím quầng 22%; "Hết phòng" nền tím lượt quét chữ trắng; "Sắp hết phòng" nền vàng chữ mực.
- **Chip mức tin cậy (bản tin):** cao = nền xanh 14% chữ xanh đậm; vừa = nền vàng 30% chữ nâu vàng.
- **Chip bằng chứng:** nền trắng, viền đường tóc, bo 8px, 12.5px; tên khách sạn đậm, số liệu chữ phụ thẳng cột.

### Cards / Containers
- **Corner Style:** 16px cho thẻ, 20px cho bảng và bản tin.
- **Background:** trắng trên nền sương; bảng chi tiết là thẻ tím than đặt ngay dưới bảng trắng.
- **Shadow Strategy:** bóng thẻ dịu (xem Elevation & Depth); thẻ biểu đồ và thẻ nhập PMS thêm viền đường tóc 1px.
- **Border:** không viền mặc định; thẻ so sánh "nóng" có viền 1.5px tím.
- **Internal Padding:** 28px cho thẻ nội dung, 22px cho bảng/biểu đồ, 16px cho mục lồng bên trong.

### Navigation
- **Thanh điều hướng:** dính trên cùng, nền tím than, cao 76px (64px dưới 720px), đường đáy trắng 6%. Logo trái; năm liên kết neo 15.5px/500 trắng 92%, hover rõ hẳn và gạch chân tím quầng lệch 6px; bên phải nút outline "Liên hệ tư vấn" và "Đăng nhập" (đổi thành "Vào dashboard" khi đã có phiên).
- **Menu gập (≤1180px):** `<details>` gốc với nút ba gạch 42px bo 10px; bảng trắng bo 14px, rộng tối thiểu 220px, bóng nổi; mục 15.5px/600 chữ mực, hover nền sương chữ tím; tự đóng khi chọn mục.
- **Liên kết bỏ qua:** nút trắng bo 8px trượt xuống khi nhận focus.

### Ảnh ghép đầu trang (signature)
Ảnh bờ biển bo 24px (tỷ lệ 16:12.6) với lớp phủ gradient xanh tím từ trái. Trên ảnh: thanh điều hướng giả của dashboard (chữ hoa 12.5px), viên thuốc thông báo trắng có chuông tím, bảng phòng mini kính mờ (viền trắng 70%, 7 đêm × 5 khách sạn, ô 14px bo 3px, đêm đỉnh có viền trắng), tiêu đề số lớn "N/4 đối thủ hết phòng". Tràn ra ngoài ảnh: hộp bản tin nền `#eceefb` viền tím quầng bo 16px ở góc dưới trái, chữ gõ từng ký tự có con trỏ tím nhấp nháy; công tắc lượt quét viên thuốc trắng 86% kính ở mép phải, lượt đang chạy nền tím. Toàn bộ là mảnh giao diện, `aria-hidden` hoặc có nhãn "Minh hoạ" cho trình đọc màn hình.

**Chuyển động:** bản tin gõ lại một lần (26ms/ký tự); lượt quét chạy vòng mỗi 2.6 giây 06:00 → 14:00 → 22:00, ô bảng đổi màu trong 0.5s với easing `cubic-bezier(0.16, 1, 0.3, 1)`. Khi `prefers-reduced-motion: reduce`, bản tin hiện đủ ngay, lượt quét đứng yên, mọi transition và animation trên trang bị tắt, cuộn mượt cũng tắt.

### Thanh thông số (trust facts)
Hàng ngang căn giữa dưới đầu trang, đường trên trắng 10%, chữ 15.5px mờ trên nền tối với phần nhấn đậm trắng, mỗi mục có chấm xanh 8px. Chỉ nêu thông số dịch vụ có thật (3 lượt quét, 30 đêm, 9 loại sự kiện, nguồn Booking.com), không số khách hàng hay đánh giá. Xếp dọc dưới 720px.

### Thẻ so sánh
Hai thẻ trắng cạnh nhau (tối đa 920px): tên, huy hiệu, giá 32px thẳng cột, lưới 16 ô phòng (8 cột, ô 26px bo 5px; ô còn = vàng, ô đã bán = tím đậm), câu kết luận. Thẻ "nóng" viền tím và huy hiệu vàng.

### Dải chú giải mức tin cậy
Một thẻ trắng chia 4 cột bằng đường tóc dọc; mỗi cột: mẫu ô 36px bo 9px, tên 16.5px/700, mô tả 14.5px chữ phụ. 2×2 dưới 1000px, một cột dưới 720px.

### Dòng thời gian
Năm bước trên thanh ngang 3px gradient tím → tím quầng; mỗi bước: giờ (22px/800 tím; hai bước giữa 15px/700 chữ phụ vì là mốc tương đối), điểm dừng tròn 44px nền trắng viền tím 3px có quầng trắng 6px che thanh, tiêu đề 18px, mô tả 15px. Dưới 1000px chuyển thành thanh dọc bên trái.

### Bảng 30 đêm + bảng chi tiết (signature)
- **Bảng:** thẻ trắng bo 20px; đầu thẻ có tiêu đề 17px và meta "Lượt quét gần nhất" với chấm xanh có quầng. Bảng `border-spacing: 3px`, ô là nút 32px bo 6px mang một trong bốn dấu, số 12.5px/700; hàng "Của bạn" trên cùng có chấm xanh và vạch ngăn; đầu cột thứ/ngày, cuối tuần chữ tím. Hai hàng compset cuối: thước đo cột tím (tỷ lệ đối thủ hết phòng) và giá thấp nhất (nghìn ₫). Chú giải bốn dấu dưới đường tóc.
- **Trạng thái ô:** hover nhấc 1px kèm bóng; đang chọn (`aria-pressed`) viền 2.5px tím lệch 2px.
- **Bảng chi tiết:** tím than bo 20px, 3 cột (loại phòng / sáu lượt quan sát / sự kiện), cập nhật `aria-live`. Nhãn trạng thái viên thuốc (vàng khi còn phòng, tím quầng khi hết). Sáu lượt quan sát là sáu ô "bản in" mờ dần theo tuổi (lượt cũ nhạt hơn). Hàng hết phòng mờ 55%.

### Thẻ bản tin
Thẻ trắng bo 20px, bóng nổi sâu trên nền tím than, rộng tối đa 500px, có ảnh vuông bo 20px chồng góc phải phía sau (ẩn dưới 720px). Đầu thẻ tiêu đề tím có ô vuông 10px, meta thẳng cột; tóm tắt 17px/500; mỗi nhận định là ô sương bo 12px với chip mức tin cậy, đề xuất và chip bằng chứng; hàng tín hiệu có chấm vàng (vừa) hoặc tím (cao); chân thẻ đường tóc.

### Thẻ biểu đồ + thẻ nhập PMS
Thẻ trắng viền đường tóc. Biểu đồ SVG: cột tím sữa, cột đỉnh tím; đường công suất xanh 2.6px có chấm; lưới `#ece9f7`, trục `#c9c4e4`; vạch đỉnh tím nét đứt 2/3; nhãn trục 11px/600 thẳng cột (đỉnh 800 tím). Thẻ nhập PMS: tên file, số dòng hợp lệ chữ xanh đậm, bảng lỗi theo dòng.

### Danh sách nguyên tắc
Ảnh dọc bo 24px bên trái (4fr), lưới 2 cột bên phải (8fr), mỗi mục có đường trên `#dcd7f2`, ô biểu tượng 40px bo 12px nền tím rửa với nét vẽ tím 1.8px, tiêu đề 18.5px, mô tả 15.5px. Mục cuối trải đủ hai cột. Phía trên là dải ảnh cửa sổ bo 24px, gradient tím than từ trái, câu trích trắng lớn.

### FAQ
Accordion `<details>` gốc, khe 12px, tối đa 880px. Mỗi mục: nền trắng, viền đường tóc, bo 12px; câu hỏi 17.5px/600 đệm 20px 24px, chevron tím xoay 180° trong 0.3s khi mở. Hover viền tím sữa; mở thì viền tím quầng và bóng thẻ dịu.

### Khối lời mời + thẻ kênh liên hệ
Khối tím bo 28px với hai quầng, 7fr chữ / 5fr kênh. Ba bước đánh số bằng vòng tròn trắng 18% 30px. Kênh liên hệ là thẻ trắng bo 14px: ô biểu tượng 46px nền tím rửa, nhãn 13px chữ phụ, giá trị clamp(15px, 4.2vw, 19px)/700 thẳng cột, mũi tên tím trượt 4px khi hover, thẻ nhấc 2px. Kênh chỉ hiện khi biến môi trường `CONTACT_ZALO` / `CONTACT_PHONE` / `CONTACT_EMAIL` được đặt; không có kênh nào thì hiện hộp viền đứt trắng 60% "Thông tin liên hệ đang được cập nhật."

### Chân trang
Nền tím than, logo và một câu mô tả (tối đa 36ch), liên kết trắng 2 cột, đường chia trắng 10%, dòng nhỏ 13px: tuyên bố không liên kết với Booking.com và ghi nguồn từng ảnh Unsplash.

### Khung ảnh
Ảnh màu thật qua `next/image`, cắt trong khung bo 16px (hoặc theo vị trí: 20–24px), nền chờ tím than nổi, có thể chỉnh `object-position`. Lớp phủ gradient chỉ dùng khi có chữ hoặc giao diện đặt lên ảnh.

### App · Nút
Gọn, phẳng, không nhấc.
- **Shape:** bo 8px; cao 36px, đệm ngang 14px, chữ 14px/600; bản nhỏ 32px, đệm 10px, chữ 13px. Biểu tượng 16px cách chữ 6px; khi bận thay biểu tượng bằng vòng quay 14px và đặt `aria-busy`.
- **Primary:** nền tím, chữ trắng, đế bóng 1px; hover tím đậm; nhấn lún 1px; vô hiệu tím 45%, bỏ bóng. Mỗi thao tác chính của màn hình (Đăng nhập, Thêm khách sạn, Lưu) một nút.
- **Secondary (mặc định):** nền trắng, chữ mực, vòng trong 1px đường tóc đậm; hover giấy phớt; nhấn nền lõm.
- **Danger:** nền trắng, chữ đỏ, vòng đỏ 35%; hover nền đỏ nhạt.
- **Ghost:** chữ thân, hover nền lõm chữ mực. **Quiet:** chữ tím, hover nền tím phớt chữ tím đậm.
- **Focus:** vòng 2px tím, lệch 2px, bo 6px (toàn app); trên thanh bên vòng đổi sang tím quầng.
- **Chuyển trạng thái:** 150ms cho nền, bóng, màu.

### App · Nhãn trạng thái (Badge)
Viên thuốc cao 22px, đệm 8px, chữ 12px/600, chấm 6px đứng trước (có thể tắt). Tông: xanh (xanh nhạt / xanh chữ) cho tin cậy cao và "của bạn"; đỏ (đỏ nhạt / đỏ đậm) cho lỗi; cảnh báo (vàng cảnh báo / nâu) cho giảm phòng, sắp hết, tin cậy vừa; xám (lõm / chữ phụ, chấm mờ) cho biến động giá và loại phòng; lam (tím phớt / tím đậm) cho có phòng lại; tím (tím rửa / tím đậm); tím đậm hết phòng (nền `sold-out-plum`, chữ trắng, chấm vàng) chỉ cho sự kiện "Hết phòng". Nhãn "Của bạn" cạnh tên khách sạn là viên thuốc không chấm.

### App · Ô nhập, chọn, nhóm chọn
- **Ô nhập / chọn:** cao 36px (40px trên trang đăng nhập), bo 8px, viền 1px đường tóc đậm, nền trắng, đệm 12px, chữ 14px mực, lõm 1px phía trên; placeholder chữ mờ. Focus: viền tím và vòng 3px tím 15%, không viền ngoài. Lỗi: viền đỏ, vòng đỏ 15%, câu lỗi 13px đỏ `role="alert"` ngay dưới. Vô hiệu: nền giấy phớt, chữ mờ. `<select>` bỏ giao diện gốc, mũi tên 16px chữ phụ cách phải 10px.
- **Trường:** nhãn 13px/600 chữ thân, cách ô 6px, gợi ý 12px chữ phụ.
- **Trên nền tím than (chọn tenant của operator):** viền trắng 15%, nền trắng 6%, chữ trắng; focus tím quầng.
- **Nhóm chọn (Segmented):** `radiogroup` thay cho select ngắn (14/30/60 đêm, vai trò). Rãnh lõm bo 8px đệm 2px; mỗi lựa chọn bo 6px, cao 32px, chữ 13px/600; đang bật nền trắng chữ mực có bóng nổi nhẹ; chưa bật chữ phụ, hover mực.

### App · Thẻ, dải số đo, ghi chú, trạng thái rỗng
- **Thẻ:** bo 12px, viền đường tóc, nền trắng, bóng thẻ app. Đầu thẻ (tuỳ chọn) có đường chia dưới, đệm 14px 20px: tiêu đề 15px/700, mô tả 13px chữ phụ, thao tác bên phải. Thân đệm 20px.
- **Dải số đo (StatStrip):** không phải lưới thẻ: một khối bo 12px viền đường tóc, các ô ngăn bằng khe 1px lộ nền đường tóc. Mỗi ô: nhãn 13px chữ phụ, giá trị 20px/700 thẳng cột, gợi ý 12px. Tông giá trị: mực, xanh chữ (tốt), nâu (chú ý), đỏ (xấu). Tổng quan dùng bốn ô: đêm nay của bạn, đối thủ hết phòng đêm nay, giá bạn so trung vị, số đêm bạn còn ≤3 phòng.
- **Ghi chú (Note):** bo 8px, viền 1px, đệm 10px 14px, chữ 13px, biểu tượng 16px tuỳ chọn. Trung tính (giấy phớt), thông tin (tím phớt, viền tím rửa), cảnh báo (vàng cảnh báo, chữ nâu). Không có viền màu bên trái.
- **Hộp lỗi:** bo 8px, nền đỏ nhạt, viền đỏ 20%, chữ đỏ đậm 14px, biểu tượng cảnh báo 18px, `role="alert"`.
- **Trạng thái rỗng:** khối giấy phớt bo 8px căn giữa, đệm 40px 24px (gọn 24px 16px); vòng biểu tượng 40px tím rửa, tiêu đề 15px/700, lời giải thích 14px chữ phụ tối đa 28rem nói rõ vì sao trống và làm gì tiếp, thao tác bên dưới.
- **Đang tải:** thanh skeleton bo 6px ánh sáng chạy 1.4s trên nền lõm, cùng hình khối với nội dung thật (dải số đo 92px, thẻ khách sạn 132px).

### App · Thanh bên và ngăn kéo (Navigation)
- **Thanh bên:** cố định trái, rộng 248px, nền tím than. Đầu cao 64px: logo mặt tiền 28px (ô vuông tím bo 7px, sáu ô cửa trắng, hai ô vàng) và wordmark. Dưới đó thẻ khách sạn đang xem (viền trắng 10%, bo 8px, nhãn "Khách sạn" 12px, tên 15px/600 trắng tối đa 2 dòng); operator thấy ô chọn tenant thay cho thẻ này.
- **Mục điều hướng:** cao 40px, bo 8px, đệm 12px, biểu tượng 18px cách 12px, chữ 15px/500. Chưa chọn: chữ mờ trên nền tối, biểu tượng chữ nhạt; hover nền trắng 5% chữ trắng. Đang chọn (`aria-current="page"`): nền trắng 9%, chữ trắng, biểu tượng tím quầng. Bốn mục tenant (Tổng quan, Sự kiện, Bản tin AI, Cài đặt); operator có thêm nhóm "Vận hành" (Tenant, Tài khoản, Sức khoẻ scraper) dưới nhãn 12px/600 viết thường.
- **Chân thanh bên:** đường chia trắng 8%. Khối độ mới dữ liệu nền trắng 4% bo 8px, 12px: chấm xanh 8px có quầng + "Quét lúc 14:06 hôm nay" trắng, dòng "Lượt tiếp theo 22:00" chữ nhạt; khi đang quét chấm vàng nhấp nháy "Đang quét…". Hàng người dùng: avatar tròn 32px chữ cái đầu, email 13px/500 trắng, vai trò 12px, nút đăng xuất 32px.
- **Dưới 1024px:** thanh trên tím than cao 56px dính trên cùng (nút menu 36px, logo 24px, wordmark 13px). Ngăn kéo rộng 280px (tối đa 85vw) trượt hiện 200ms, bóng nổi app, lớp phủ tím than 55% mờ 2px; đóng bằng Esc, nút đóng, bấm lớp phủ hoặc đổi trang.

### App · Đầu trang, đường dẫn, tab
- **Đầu trang:** đường dẫn (tuỳ chọn) 13px chữ phụ, ngăn bằng chevron 14px chữ mờ, mục cuối chữ thân `aria-current`; H1 24px; phụ đề 14px chữ phụ (khoảng đêm, chấm xanh "Quét lúc…"); thao tác bên phải, xuống dòng khi hẹp.
- **Tab:** hàng có đường đáy đường tóc, cuộn ngang khi hẹp; mỗi tab chữ 13→14px/600, gạch đáy 2px; đang chọn gạch tím chữ mực biểu tượng tím; chưa chọn chữ phụ, hover gạch đường tóc đậm. Số đếm là viên thuốc lõm 12px thẳng cột.

### App · Bảng
Bảng nằm trong khung cuộn ngang riêng. Đầu cột nền giấy phớt, 12px/600 chữ phụ, viết thường, đường đáy đường tóc; ô 14px chữ thân, đệm 12px 16px (bản dày 8px 12px), đường đáy đường tóc, hàng cuối không đường. Hàng có hover giấy phớt 100ms. Cột số canh phải, thẳng cột. Hàng khách sạn ngừng quét chuyển chữ mờ. Thao tác trong hàng là nút nhỏ 32px, trên điện thoại chỉ còn biểu tượng.

### App · Chồng dải khách sạn + dò một đêm (signature)
- **Chú giải:** hàng mẫu dấu 18px bo 5px, chữ 12px chữ phụ, đặt ngay trên trục.
- **Trục đêm:** dính trên cùng, nền sương 95% mờ nhẹ, bo 8px. Mỗi cột: nhãn tháng "Th 9" 11px/600 chỉ ở cột đầu và ngày mùng 1; thứ viết tắt "T6" 11px; ngày 13px/700 mực thẳng cột. Cuối tuần là ô lõm bo 6px, ngày 800 mực. Cột đang dò nền tím chữ trắng.
- **Thẻ Thị trường:** thẻ app; đầu thẻ "Thị trường" 15px/700 + "N đối thủ đang theo dõi" 13px chữ phụ, chú giải đường bên phải. Thước đo "Đối thủ hết phòng mỗi đêm": mỗi đêm một cột tím đậm (rộng tối đa 22px, cao ≥6px theo tỷ lệ, bo trên 3px) với nhãn "k/n" 11px/700; không đối thủ nào hết phòng thì thước đo thu lại thành vạch đường tóc 2px kèm câu chữ mờ. Dưới là đường giá thấp nhất mỗi đêm cao 96px: trung vị đối thủ nét liền mực 2px, thấp nhất đối thủ nét đứt 3/3 chữ mờ 1.5px, của bạn nét liền xanh đậm 2px.
- **Thẻ khách sạn:** thẻ app, đệm 14px 20px. Đầu thẻ: tên 15px/700 (liên kết, hover tím gạch dưới), tên Booking đầy đủ 12px chữ phụ nếu khác nhãn; bên phải "Đêm nay Còn 7 phòng · 4.000.577 ₫", "Giá kỳ này 3,6 Tr–5 Tr" (≥1280px) và "Chi tiết →" 13px/600 tím. Khách sạn của bạn: chấm xanh 10px có quầng trước tên, nhãn "Của bạn", viền xanh 35% cộng vòng xanh 10%, luôn đứng ngay dưới thẻ Thị trường, trên mọi đối thủ.
- **Dải ô:** mỗi đêm một ô cao 32px bo 6px, lớp `sb-mark-*`, số 13px/700 thẳng cột ("HẾT" 9px). Mỗi ô là liên kết tới chi tiết đêm với nhãn đọc đủ ("Caravelle, Thứ bảy 27/09: Còn 5 phòng (Booking báo chính xác), giá …"). Hover nhấc 1px kèm bóng nhấc ô (180ms). Ở mức khách sạn dải hiện chính xác, ẩn số, hết phòng và hai trạng thái dữ liệu; dấu "Ít nhất" xuất hiện ở mức loại phòng (chi tiết đêm, dòng sự kiện).
- **Đường giá trên dải:** cao 60px, thang y riêng của từng thẻ, hai nhãn mức giá 10.5px/600 chữ phụ trên nền trắng bo 4px viền lưới. Thẻ của bạn: giá của bạn liền xanh đậm 2px, trung vị đối thủ đứt mực 1.25px. Thẻ đối thủ: giá khách sạn đó liền mực 2px, giá của bạn đứt xanh 1.25px. Điểm lẻ không có hàng xóm vẫn vẽ chấm 2.5px. Thẻ của bạn có thêm hàng "Công suất theo PMS" 11px/600 dưới dải khi có dữ liệu.
- **Dò một đêm:** rê chuột hoặc Tab vào bất kỳ ô, cột thước đo hay vùng biểu đồ của một đêm: cột trục đổi nền tím, mọi ô cùng đêm có viền 2px tím lệch 1px, mọi biểu đồ có dải tím 12% rộng đúng một cột và chấm 4px viền trắng trên từng đường. Bảng đọc nổi rộng 300px (bo 12px, viền đường tóc, bóng nổi app, hiện dần 120ms, không bắt chuột) mở cạnh cột và lật sang trái khi sát mép: đầu "Thứ bảy 27/09" 15px/700 + "Đêm nay / Đêm mai / N ngày nữa" 12px; mỗi khách sạn một hàng (mẫu dấu 20px, tên 13px, giá gọn thẳng cột; khách sạn của bạn đậm xanh chữ); dưới đường tóc: đối thủ hết phòng k/n, trung vị đối thủ, giá bạn so trung vị ±%, công suất PMS. Rời khỏi chồng thì tắt.

### App · Biểu đồ đường
SVG vẽ theo chiều rộng thật để chữ trục giữ đúng cỡ. Nét 2px bo tròn đầu; điểm bán kính 4px (6px khi rê) có vòng trắng 2px; điểm là mức sàn vẽ rỗng (nền trắng, viền màu chuỗi). Lưới ngang `chart-grid`, đường đáy đường tóc đậm, nhãn trục 11px/600 chữ phụ thẳng cột, lề trái 56px. Rê chuột: vạch dọc tím 30% nét đứt, các chuỗi khác mờ còn 35% trong 150ms, chú thích trắng bo 8px bóng nổi 12px. Chú giải luôn có khi ≥2 chuỗi (vạch 16×2px). Mỗi điểm nhận focus bằng bàn phím với nhãn đọc đủ. Không có dữ liệu: khối giấy phớt bo 8px với câu giải thích.

### App · Dòng sự kiện
Nhóm theo lượt quét, mới nhất trước. Tiêu đề nhóm dính (giấy phớt 95%, mờ nhẹ, đường tóc trên dưới, 12px/600): "Lượt quét 14:06 hôm nay · 6 thay đổi" (giờ, không phải mã). Mỗi dòng đọc thành câu: nhãn loại → khách sạn (600 mực, liên kết) · loại phòng (hoặc "Toàn khách sạn") → "Đêm 26/09" (13px/600 tím, liên kết tới chi tiết đêm) → thay đổi "7 → 6 phòng" thẳng cột với chip biến động bo 6px 12px/700 (giảm: vàng cảnh báo chữ nâu; tăng: lõm chữ mực; mũi tên 12px; dấu trừ thật "−"). Mức tin cậy chỉ hiện khi khác "chính xác" (mẫu dấu 12px + nhãn 12px), "ít nhất" hiện bằng "≥" ngay trong số. Dòng được trỏ tới qua liên kết: nền tím phớt, vòng trong 2px tím 40%, cuộn vào giữa.

### App · Bản tin AI
Tóm tắt 17px/1.7 mực tối đa 70ch mở đầu. Các phần (Điểm nổi bật, Tín hiệu nhu cầu, Gợi ý giá, Rủi ro) có tiêu đề 17px/700 kèm số đếm; phần trống là hộp giấy phớt một câu. Nhận định là thẻ app đệm 20px: tiêu đề 15px/700 tối đa 60ch, nhãn mức tin cậy (cao xanh, vừa cảnh báo, thấp xám), hộp "Đề xuất" tím phớt bo 8px, rồi hàng chip bằng chứng: "Bằng chứng" 12px/600 chữ phụ, mỗi chip cao 28px bo 8px nền trắng vòng đường tóc, 12px/500 thẳng cột, có biểu tượng; chip nào trỏ được tới đêm hay sự kiện thì là liên kết, hover tím phớt chữ tím đậm. Cuối trang hai `<details>`: "Giới hạn của dữ liệu" (thẻ viền đường tóc) và "Nhận định bị loại" (viền đứt đường tóc đậm trên giấy phớt, lý do loại chữ đỏ đậm). Số token, chi phí và mô hình chỉ operator thấy.

### App · Đăng nhập
Hai nửa trên máy tính: nửa tím than 45% (tối đa 640px) và nửa biểu mẫu trắng; trên điện thoại nửa tối thu thành dải trên cùng. Nửa tối: logo 30px + wordmark, liên kết "Về trang giới thiệu"; câu thương hiệu trắng 600 (28px, 1.25, −0.015em trên máy tính), câu phụ 14px chữ mờ; bảng minh hoạ 7 đêm × 4 khách sạn (ô 24px bo 5px, bốn dấu, khung trắng 4% viền trắng 10% bo 12px, ghi "Minh hoạ", `aria-hidden` kèm câu cho trình đọc màn hình); dòng 12px "Không liên kết với Booking.com". Nửa trắng: biểu mẫu tối đa 380px, H1 "Đăng nhập" 24px, ô nhập cao 40px, nút hiện/ẩn mật khẩu 32px trong ô, nút chính rộng đủ cao 40px có biểu tượng khoá, dòng chân sau đường tóc.

### App · Chuyển động
Chuyển trạng thái 150ms (hover hàng 100ms, trục đêm 100ms); nhấc ô 180ms và mọi chuyển động vào dùng `cubic-bezier(0.16, 1, 0.3, 1)`; bảng đọc hiện dần 120ms (mờ → rõ, trồi 2px); ngăn kéo 200ms; skeleton 1.4s tuyến tính; chấm "Đang quét…" nhấp nháy. Khi `prefers-reduced-motion: reduce`, mọi animation và transition về gần 0, cuộn mượt tắt.

## Do's and Don'ts

### Do:
- **Nên** ghi rõ mọi bảng, biểu đồ, bản tin dựng từ dữ liệu tổng hợp là minh hoạ, bằng dòng ghi chú 13px chữ phụ ngay dưới nó, và dùng tên khách sạn giả định.
- **Nên** hiển thị mọi con số phòng còn với một trong bốn dấu (vàng, sọc tím nhạt, viền đứt, tím đậm "HẾT") và đặt chú giải cạnh bảng.
- **Nên** đặt khách sạn của người xem ở hàng trên cùng, đánh dấu chấm xanh, tách khỏi đối thủ bằng một đường tóc.
- **Nên** giữ một nút chính tím bo 8px cho mỗi khối, hành động chính luôn là liên hệ ("Liên hệ tư vấn") (trang giới thiệu).
- **Nên** xen kẽ nền sương `#f4f5ff` và trắng giữa các phần sáng, thẻ trắng bo 16–20px với bóng thẻ dịu (trang giới thiệu).
- **Nên** dùng `tabular-nums` cho mọi số liệu, giờ, giá.
- **Nên** giữ vòng focus 2px lệch 3px, đổi màu theo nền (tím trên sáng, tím quầng trên tím than, trắng trên khối tím) (trang giới thiệu); trong app vòng 2px tím lệch 2px bo 6px, tím quầng trên thanh bên.
- **Nên** tắt mọi chuyển động khi `prefers-reduced-motion: reduce` và cho nội dung động hiện trạng thái cuối ngay.
- **Nên** ghi nguồn ảnh Unsplash ở chân trang cho mọi ảnh đang dùng.
- **Nên** dựng màn hình app trên nền sương với thẻ trắng bo 12px, viền đường tóc 1px và bóng thẻ app; khung là thanh bên tím than 248px.
- **Nên** đặt mọi dữ liệu theo đêm trên một lưới cột chung (khe 3px, tối thiểu 28px/cột) để "dò một đêm" sáng đúng một cột qua cả chồng.
- **Nên** tô ô "Chính xác" theo độ khan: ≤3 vàng đặc, 4–10 vàng vừa, >10 vàng nhạt.
- **Nên** in cuối tuần bằng chữ mực đậm trên ô lõm, không tím.
- **Nên** viết nhãn bằng lời của chủ khách sạn ("Còn 7 phòng", "Đêm 26/09", "Thấp hơn 47%") và gói thông tin kỹ thuật trong khối "Chi tiết kỹ thuật" chỉ operator thấy.
- **Nên** dùng bảng chuỗi `series-1…8` theo thứ tự cố định cho biểu đồ nhiều loại phòng và vẽ mức sàn bằng chấm rỗng.
- **Nên** cho mỗi trạng thái rỗng một câu nói vì sao trống và một bước tiếp theo.

### Don't:
- **Không** bịa giá, số đánh giá, sao, lời chứng thực, logo khách hàng, số liệu hiệu quả hay cam kết hoàn tiền; chỗ giá luôn là "Liên hệ báo giá".
- **Không** dùng logo, màu nhận diện hay tên Hostinger; không dùng logo hay nhận diện Booking.com, chỉ nhắc tên để nói nguồn dữ liệu.
- **Không** dùng vàng, tím đậm, sọc hay viền đứt để trang trí; bốn dấu đó chỉ mang nghĩa mức tin cậy.
- **Không** hiện kênh liên hệ với số hay email chưa cấu hình; thiếu thì hiện hộp "đang được cập nhật".
- **Không** thêm họ chữ thứ hai.
- **Không** đặt quầng tím trên phần sáng, không dùng kính mờ ngoài ảnh ghép đầu trang.
- **Không** đặt nhãn chữ hoa nhỏ (kicker, eyebrow) phía trên tiêu đề phần; đầu phần chỉ gồm H2 và đoạn dẫn.
- **Không** để trang cuộn ngang; bảng rộng cuộn trong khung của chính nó.
- **Không** mang bo 16–28px, quầng tím, quầng nút, nhấc nút hay kính mờ của trang giới thiệu vào màn hình app; app dừng ở bo 12px và bóng thẻ dịu.
- **Không** dựng Tổng quan thành bảng số trơn hay lưới thẻ KPI kiểu admin Tailwind mặc định; mỗi khách sạn là một dải mang dấu.
- **Không** dùng xanh lá hay vàng trong chuỗi biểu đồ; trong app không tô tím cho cuối tuần hay để trang trí.
- **Không** cho tenant thấy token, chi phí, mô hình AI, probe, chặn, lỗi hay mã lượt quét.
- **Không** dùng giá trị hex rời (`bg-[#…]`) trong app; màu mới phải thành biến `--sb-*` trước.
- **Không** dùng viền màu bên trái cho ghi chú hay thẻ.
